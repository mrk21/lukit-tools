# プロジェクト名

プロジェクトが何であり、何を解決するのかを 1〜3 行で。必要なら主要機能を箇条書きで続ける。

- 主要機能 1
- 主要機能 2

## 技術スタック

`<技術名>: <バージョン>` の形で列挙する。行頭には「言語 / フレームワーク」等のカテゴリ名ではなく**実際の技術名**（例: `TypeScript`・`Vite`）を置く。バージョンが無いものは省略可、補足はサブ箇条書きに。

- <言語>: `<x.y>`
- <フレームワーク>: `<x.y>`
- <ビルド / タスクランナー>: `<x.y>`
  - 補足があればここに
- <主要ライブラリ・基盤>: `<x.y>`

## セットアップ

動作要件（対応 OS・必要ランタイム/SDK など）と、動かせるようになるまでの手順(なるべくCLIベースで書くと楽)。

```sh
command1
command2
```

## 使用方法

エンドユーザーとしての使い方。CLI ならサブコマンド例、GUI なら操作手順やホットキーなど。

1. 手順1
2. 手順2

## 開発方法

### 開発コマンド

```sh
# ツールAの説明
tool-a arg1

# ツールBの説明
tool-b
```

### ディレクトリ構成

```
src/
├── a/   # 役割の説明
└── b    # 役割の説明
```

## 詳細・ドキュメント

背景・設計・仕組みの深掘りといった「読み物」は README に詰め込まず、`docs/` に置いてここからリンクする。

- [〇〇の仕組み](docs/xxx.md)
